spring:
  application:
    name: order-app-back

  datasource:
    # url: ${DB_URL}
    # username: ${DB_USER}
    # password: ${DB_PASSWORD}

    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

server:
  port: ${PORT:8080}

# management:
#   endpoints:
#     web:
#       exposure:
#         include: health,info

#   endpoint:
#     health:
#       show-details: always